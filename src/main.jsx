var React = require('react');
var ReactDOM = require('react-dom');
var ForecastPanel = require('./components/ForecastPanel.jsx');

ReactDOM.render(<ForecastPanel />, document.getElementById('main'));
